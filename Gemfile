source 'https://rubygems.org'

gem 'sinatra', '~> 2.0.1'
gem 'pry'
gem 'jwt'
gem 'rest-client'

group :test do
    gem 'rspec'
    gem 'rack-test', :require => "rack/test"
    gem 'rack-jwt'
    # gem 'simplecov', :require => false
end
